# ROS2ARM
Motion planing module fot our arm(lebot)

The files have not been built yet. Before use, build the workspace in the ROS2ARM directory. Do not build inside the src directory. You must build in the ROS2ARM directory.

My username in ubuntu is "wzk",so when you use the command which include the file path (eg. "ros2 launch urdf_tutorial display.launch.py model:=/home/wzk/ARM/src/arm_description/urdf/arm.urdf")

plese change your username instad of "wzk" 

Some command is written in readme of src folder, when input the pose, must make sure it is a valid pose
